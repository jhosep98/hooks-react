# useForm Hook

> How to use the useForm.

## use

example:

```
const initialForm = {
    name: "",
    age:0,
    email:""
}

const [ formValues, handleInputChange, reset ]= useForm(initialForm);

```
